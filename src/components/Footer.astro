---
import { Instagram, Facebook, Twitter, Heart, Mail, Phone, MapPin } from 'lucide-astro';
---

<footer class="relative mt-20 overflow-hidden">
  <!-- Efectos de fondo -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div class="absolute bottom-0 left-1/2 -translate-x-1/2 w-full h-80 bg-accent/10 rounded-full filter blur-3xl opacity-30"></div>
  </div>

  <div class="relative glass-footer border-t border-white/10 pt-16 pb-10">
    <div class="max-w-7xl mx-auto px-4">
      <!-- Sección principal del footer -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-10 mb-12">
        <!-- Logo y descripción -->
        <div class="space-y-4">
          <div class="flex items-center space-x-3">
            <div class="w-10 h-10 bg-accent/20 rounded-full flex items-center justify-center">
              <Heart size={20} class="text-accent" />
            </div>
            <span class="text-xl font-display text-surface-light tracking-wider">Nadie Lo Sabrá</span>
          </div>
          <p class="text-surface-light/70 text-sm leading-relaxed">
            Tu espacio discreto y elegante para conectar con personas especiales. Siempre manteniendo la privacidad como prioridad.
          </p>
          <div class="flex space-x-4 pt-2">
            <a href="#" class="w-8 h-8 rounded-full bg-white/5 flex items-center justify-center text-surface-light/70 hover:text-accent hover:bg-white/10 transition-colors">
              <Instagram size={16} />
            </a>
            <a href="#" class="w-8 h-8 rounded-full bg-white/5 flex items-center justify-center text-surface-light/70 hover:text-accent hover:bg-white/10 transition-colors">
              <Facebook size={16} />
            </a>
            <a href="#" class="w-8 h-8 rounded-full bg-white/5 flex items-center justify-center text-surface-light/70 hover:text-accent hover:bg-white/10 transition-colors">
              <Twitter size={16} />
            </a>
          </div>
        </div>

        <!-- Enlaces rápidos -->
        <div>
          <h3 class="text-surface-light font-medium mb-5 text-lg">Enlaces Rápidos</h3>
          <ul class="space-y-3">
            {[
              { label: 'Inicio', href: '/' },
              { label: 'Explorar', href: '/explorar' },
              { label: 'Premium', href: '/premium' },
              { label: 'Blog', href: '/blog' },
              { label: 'Anúnciate Gratis', href: '/anunciate-gratis' }
            ].map(link => (
              <li>
                <a href={link.href} class="text-surface-light/70 hover:text-accent transition-colors text-sm flex items-center">
                  <span class="w-1.5 h-1.5 bg-accent/50 rounded-full mr-2"></span>
                  {link.label}
                </a>
              </li>
            ))}
          </ul>
        </div>

        <!-- Contacto -->
        <div>
          <h3 class="text-surface-light font-medium mb-5 text-lg">Contacto</h3>
          <ul class="space-y-4">
            <li class="flex items-start">
              <Mail size={16} class="text-accent mt-0.5 mr-3 flex-shrink-0" />
              <span class="text-surface-light/70 text-sm">contacto@nadielosabrá.com</span>
            </li>
            <li class="flex items-start">
              <Phone size={16} class="text-accent mt-0.5 mr-3 flex-shrink-0" />
              <span class="text-surface-light/70 text-sm">+56 9 1234 5678</span>
            </li>
            <li class="flex items-start">
              <MapPin size={16} class="text-accent mt-0.5 mr-3 flex-shrink-0" />
              <span class="text-surface-light/70 text-sm">Santiago, Chile</span>
            </li>
          </ul>
        </div>

        <!-- Newsletter -->
        <div>
          <h3 class="text-surface-light font-medium mb-5 text-lg">Newsletter</h3>
          <p class="text-surface-light/70 text-sm mb-4">Recibe noticias y ofertas exclusivas</p>
          <form class="space-y-3">
            <div class="relative">
              <input 
                type="email" 
                placeholder="Tu email" 
                class="w-full bg-black/20 border border-white/10 rounded-lg py-2.5 px-4 text-surface-light text-sm placeholder:text-surface-light/50 focus:ring-2 focus:ring-accent/50 focus:outline-none"
              />
            </div>
            <button type="submit" class="w-full py-2.5 bg-accent/80 hover:bg-accent text-white text-sm rounded-lg transition-colors">
              Suscribirme
            </button>
          </form>
        </div>
      </div>

      <!-- Línea divisoria -->
      <div class="h-px bg-gradient-to-r from-transparent via-white/10 to-transparent my-8"></div>

      <!-- Copyright -->
      <div class="flex flex-col md:flex-row justify-between items-center text-sm text-surface-light/60">
        <p>&copy; {new Date().getFullYear()} Nadie Lo Sabrá. Todos los derechos reservados.</p>
        <div class="flex space-x-6 mt-4 md:mt-0">
          <a href="#" class="hover:text-accent transition-colors">Términos de Servicio</a>
          <a href="#" class="hover:text-accent transition-colors">Política de Privacidad</a>
        </div>
      </div>
    </div>
  </div>
</footer>

<style>
  .glass-footer {
    background: rgba(0, 0, 0, 0.3);
    backdrop-filter: blur(20px);
  }
</style>
